name: Conda Workflow

on:
  push:
    branches:
      - main
      - test-ci
  pull_request:
    branches:
      - main
      - test-ci

jobs:
  setup_environment:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v2
        with:
          auto-update-conda: true

      - name: Create Conda environment
        run: conda env create --name buildcv --file environment.yml

      - name: Set up TeX Live
        uses: xu-cheng/texlive-action/full@v1
        with:
          tex-packages: pdftex

      - name: Run Python script
        run: conda run --name buildcv bash -c "python makeCV.py --connected --testing"

      - name: Run LaTeX
        run: conda run --name buildcv bash -c "python makeCV.py --compiling"

      - name: Prepare LaTeX files
        run: |
          # Move generated files to a separate directory
          mkdir pdfs/
          mv *pdfs build/

      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        with:
            name: artifacts
            path: build/

